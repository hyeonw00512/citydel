services:
  - type: web
    runtime: node
    name: citydel-game
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start -w @citadel/server
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 20
